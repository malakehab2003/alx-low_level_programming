#include <stdio.h>
#include <unistd.h>

/**
 * _isalpha - check if the char c is alpha or not
 *
 * Return: 1 if the char is alpha
 * Return: 0 otherwise
*/

int _isalpha(int c)
{
	int i = ;
	while
}
