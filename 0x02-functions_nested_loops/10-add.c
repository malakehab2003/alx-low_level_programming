#include <stdio.h>
#include <unistd.h>
#include "main.h"

/**
 * add - Add two numbers
 *
 * Return: the sum of the number
 *
 *@x:number to sum
 *@y:number to sum
*/

int add(int x, int y)
{
	int r;

	r = x + y;
	return (r);
}
