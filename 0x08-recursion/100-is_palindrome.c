#include "main.h"

/**
 * is_palindrome - check if the string is empty
 *
 * Return: 1 if empty
 *
 * @s: the string
*/

int is_palindrome(char *s)
{
	if (*s == 0)
		return (1);
	else
		return (0);
}
